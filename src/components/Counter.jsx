//import { useState } from "react";
//setCount
export default function Counter({ displayValue, onTofu }) {
  //   const [count, setCount] = useState(0);
  return <button onClick={onTofu}>count is {displayValue}</button>;
}
